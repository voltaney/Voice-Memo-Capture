[package]
name = "voice-memo-capture"
version = "0.1.0"
edition = "2024"

[dependencies]
# 録音・デバイス列挙
cpal = "0.18.1"
# WAV 書出し
hound = "3.5.1"
# ウィンドウUI（retained-mode。ソフトウェアレンダラでGPU初期化不要＝起動が軽い）
slint = { version = "1.17", default-features = false, features = [
    "compat-1-2",
    "std",
    "backend-winit",
    "renderer-software",
    "accessibility",
] }
# n8n Webhook への blocking POST（Basic認証は basic_auth に委譲）
reqwest = { version = "0.13.4", features = ["blocking", "json"] }
# .env 読込
dotenvy = "0.15.7"
# Opus エンコード（libopus の安全バインディング。audiopus_sys が libopus を同梱ビルドする）
audiopus = "0.2.0"
# Ogg コンテナへの多重化（OpusHead / OpusTags / 音声パケットをページに束ねる）
ogg = "0.9.2"

# メインモニタのサイズ取得（ウィンドウ中央配置用の GetSystemMetrics）
[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_UI_WindowsAndMessaging"] }

[build-dependencies]
# ui/app.slint をビルド時にRustコードへコンパイルする
slint-build = "1.17"
# exe に アプリアイコン（assets/icon.ico）を埋め込む
winresource = "0.1"
